CREATE TABLE [db_FarLogistic].[dlPointAction] (
    [dlPointActionID] INT          IDENTITY (1, 1) NOT NULL,
    [Name]            VARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([dlPointActionID] ASC)
);

